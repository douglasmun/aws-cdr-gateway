# Local CDR service (app.py) dependencies — NOT part of the Lambda layer.
# Install on top of requirements.txt for the FastAPI local variant:
#   pip install -r requirements.txt -r requirements-local.txt
# The Lambda build (template.yaml) uses requirements.txt only, so these never bloat the
# deployed package.

fastapi==0.141.1         # local HTTP CDR service framework
uvicorn==0.52.1          # ASGI server for app.py
python-multipart==0.0.32 # multipart/form-data parsing for file uploads
httpx2==2.9.1            # Starlette/FastAPI TestClient dependency (test_cdr_local.py); httpx is deprecated for the TestClient
